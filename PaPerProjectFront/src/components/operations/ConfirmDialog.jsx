// Re-export shim — the canonical component now lives in components/common.
// Kept here so existing imports under operations/ keep working without churn.
export { default } from '@/components/common/ConfirmDialog';
